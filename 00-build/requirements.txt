# Cortex starter, the transparent agent your coding agent builds with you.
# Pinned to compatible ranges; bump deliberately.
anthropic>=0.69     # the model client in agent.py / critic.py
python-dotenv>=1.0  # loads .env so ANTHROPIC_API_KEY etc. are picked up
